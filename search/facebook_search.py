def search_facebook(keyword, max_results=20):
    # Placeholder adapter: direct scraping of Facebook is intentionally not
    # performed. Use an authorized API/provider and map its results to the
    # common schema returned by the other adapters.
    return []
